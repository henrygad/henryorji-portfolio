export interface Global {
    content: React.ReactElement
    flex_direction?: string
    flex?: string
    justify_Content?: string
    align_content?: string
    animationTimeDalay?: number
    onScroll?: {
      id: string
      dispaly: boolean,
      reverse?: boolean}
}